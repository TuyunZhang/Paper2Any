-r requirements-base.txt

# paper 系（NLP / 文本 / LLM 实验）特有或易变更依赖，请在这里追加或覆盖版本
# 示例（按需解除注释并改成你期望的版本）：
# transformers==4.46.0
# datasets==3.2.0
# evaluate==0.4.3
# vendi-score==0.0.3


# system dependency:
# tectonic (TeX engine) must be installed via conda-forge/apt/cargo

#sudo apt-get update
# sudo apt-get install -y inkscape
# sudo apt-get install -y tectonic

# Paper2Figure
pdfplumber
transformers==4.57.3
# mineru[core]
mineru-vl-utils
python-pptx
PyPDF2
kornia 
timm
lmdb>=1.4.1

cairosvg
vtracer
ultralytics
transformers
scikit-image
pdf2image
imgaug>=0.4.0

pillow
# pytesseract  # 已改用 PaddleOCR

# Paddle OCR (CPU)
paddlepaddle==3.2.0
paddleocr==2.9.1

PyMuPDF
img2pdf
reportlab
imgkit==1.2.3